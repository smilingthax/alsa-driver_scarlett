--- ../../alsa-kernel/pci/ac97/ac97_codec.c	2007-08-30 10:33:38.000000000 +0200
+++ ac97_codec.c	2007-09-12 00:42:33.000000000 +0200
@@ -35,9 +35,9 @@
 #include <sound/ac97_codec.h>
 #include <sound/asoundef.h>
 #include <sound/initval.h>
-#include "ac97_id.h"
+#include "../../alsa-kernel/pci/ac97/ac97_id.h"
 
-#include "ac97_patch.c"
+#include "../../alsa-kernel/pci/ac97/ac97_patch.c"
 
 MODULE_AUTHOR("Jaroslav Kysela <perex@suse.cz>");
 MODULE_DESCRIPTION("Universal interface for Audio Codec '97");
@@ -1904,6 +1904,7 @@
 
 EXPORT_SYMBOL(snd_ac97_bus);
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 0)
 /* stop no dev release warning */
 static void ac97_device_release(struct device * dev)
 {
@@ -1915,7 +1916,9 @@
 	struct snd_ac97 *ac97 = device->device_data;
 	int err;
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 14)
 	ac97->dev.bus = &ac97_bus_type;
+#endif
 	ac97->dev.parent = ac97->bus->card->dev;
 	ac97->dev.release = ac97_device_release;
 	snprintf(ac97->dev.bus_id, BUS_ID_SIZE, "%d-%d:%s",
@@ -1937,6 +1940,10 @@
 		device_unregister(&ac97->dev);
 	return 0;
 }
+#else /* <= 2.6.0 */
+#define snd_ac97_dev_register	NULL
+#define snd_ac97_dev_disconnect	NULL
+#endif
 
 /* build_ops to do nothing */
 static struct snd_ac97_build_ops null_build_ops;
