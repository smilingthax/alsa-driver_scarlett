--- ../../alsa-kernel/pci/hda/hda_codec.c	2006-07-27 12:53:14.000000000 +0200
+++ hda_codec.c	2006-07-27 15:43:15.000000000 +0200
@@ -260,7 +260,10 @@
 	unsol->queue[wp] = res;
 	unsol->queue[wp + 1] = res_ex;
 
+/* 2.4 kernels seem to have a problem with workq wrapper... */
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 0)
 	queue_work(unsol->workq, &unsol->work);
+#endif
 
 	return 0;
 }
@@ -296,6 +299,7 @@
  */
 static int init_unsol_queue(struct hda_bus *bus)
 {
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 0)
 	struct hda_bus_unsolicited *unsol;
 
 	if (bus->unsol) /* already initialized */
@@ -314,6 +318,7 @@
 	}
 	INIT_WORK(&unsol->work, process_unsol_events, bus);
 	bus->unsol = unsol;
+#endif
 	return 0;
 }
 
@@ -329,7 +334,9 @@
 	if (! bus)
 		return 0;
 	if (bus->unsol) {
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 0)
 		destroy_workqueue(bus->unsol->workq);
+#endif
 		kfree(bus->unsol);
 	}
 	list_for_each_safe(p, n, &bus->codec_list) {
